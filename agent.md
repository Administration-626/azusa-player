## 1. 全局规则 (Global Rules)
**核心原则：明确“不希望 AI 做的事”。**

- **严格的预确认机制**：在执行任何非查询类任务（如修改代码、新增文件、删除内容）之前，必须先明确向用户陈述**任务内容**、**实施方案**以及**任务边界**。同时，**必须提供一个清晰的代办清单 (TODO List)**，列出具体的执行步骤，并等待用户确认。
  - *动态变更熔断*：如果在执行过程中发现隐藏依赖，导致必须修改原 TODO 清单以外的文件或改变步骤，**必须立即中断执行**。严禁擅自“顺便”修复，必须向用户展示变更增量（Delta-TODO），重新取得明确授权后方可继续。
- **防止过度开发**：仅执行用户明确授权的操作，严禁擅自扩大修改范围或进行无关的重构。**绝对不要修改本次任务以外的文件。**
- **拒绝隐式假设**：遇到不确定的路径或系统环境依赖时，优先向用户提问澄清，禁止自行编造或猜测。
  - *灰色地带决策*：无定义/无前例风格问题 -> 用官方最佳实践。不卡死提问。决策必须显式写在 TODO 方案备注中 -> 用户可一票否决。

## 2. 项目规则 (Project Rules)
**核心原则：明确当前项目的业务边界、架构规范和常用命令。**
> 注：此为通用模板。当你将此文件复制到新项目时，请根据该项目的实际情况替换下方 `[TODO]` 的内容。

- **项目边界与禁区**：[TODO: 补充该项目绝不能修改的核心模块或禁止的行为，例如“禁止修改 /core/ 基础框架”或“禁止明文记录 Token”]。
  - *禁区冲突熔断*：若要完成当前任务，客观上**必须**修改项目禁区（或违反项目规则）时，Agent **必须立即拒绝执行该任务**。应向用户明确指出：“该问题的根源位于禁区 `[具体路径]` 内，受项目规则约束我无法修改，请提供单次豁免授权或由人工处理。”
- **代码与架构规范**：[TODO: 补充特定语言或框架的要求，例如“强制使用 TypeScript 强类型”或“遵循 PEP8 规范”]。

## 3. 架构优化指令 (Architecture Review Trigger)
- **触发条件**：用户请求架构改进/重构/提高可测试性 -> 启动 `improve-codebase-architecture` 流程。
- **分析术语**：必须严格使用 `Module`, `Interface`, `Implementation`, `Depth` (Deep/Shallow), `Seam`, `Adapter`, `Leverage`, `Locality` 词汇。
- **输出物约束**：禁止直接在当前仓库生成报告。必须在 Windows `%TEMP%` 目录生成 `architecture-review-<timestamp>.html` 报告，用 `start <path>` 命令自动打开，并通知用户绝对路径。

## 4. 四个原则 (Four Principles)

四个原则，集中在一个文件中，直接解决这些问题：

| 原则 | 解决什么问题 |
| --- | --- |
| 编码前思考 | 错误假设、隐藏困惑、缺少权衡 |
| 简洁优先 | 过度复杂、臃肿抽象 |
| 精准修改 | 无关编辑、触碰不应碰的代码 |
| 目标驱动执行 | 通过测试优先、可验证的成功标准 |

### 4.1 编码前思考
**不要假设。不要隐藏困惑。呈现权衡。**

LLM 经常默默选择一种解释然后执行。这个原则强制明确推理：
- **明确说明假设** — 如果不确定，询问而不是猜测
- **呈现多种解释** — 当存在歧义时，不要默默选择
- **适时提出异议** — 如果存在更简单的方法，说出来
- **困惑时停下来** — 指出不清楚的地方并要求澄清

### 4.2 简洁优先
**用最少的代码解决问题。不要过度推测。**

对抗过度工程的倾向：
- 不要添加要求之外的功能
- 不要为一次性代码创建抽象
- 不要添加未要求的"灵活性"或"可配置性"
- 不要为不可能发生的场景做错误处理
- 如果 200 行代码可以写成 50 行，重写它
> **检验标准：** 资深工程师会觉得这过于复杂吗？如果是，简化。

### 4.3 精准修改
**只碰必须碰的。只清理自己造成的混乱。**

编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式
- 不要重构没坏的东西
- 匹配现有风格，即使你更倾向于不同的写法
- 如果注意到无关的死代码，提一下 —— 不要删除它

当你的改动产生孤儿代码时：
- 删除因你的改动而变得无用的导入/变量/函数
- 不要删除预先存在的死代码，除非被要求
> **检验标准：** 每一行修改都应该能直接追溯到用户的请求。

### 4.4 目标驱动执行
**定义成功标准。循环验证直到达成。**

将指令式任务转化为可验证的目标：

| 不要这样做... | 转化为... |
| --- | --- |
| "添加验证" | "为无效输入编写测试，然后让它们通过" |
| "修复 bug" | "编写重现 bug 的测试，然后让它通过" |
| "重构 X" | "确保重构前后测试都能通过" |

对于多步骤任务，说明一个简短的计划：
1. `[步骤]` → 验证: `[检查]`
2. `[步骤]` → 验证: `[检查]`
3. `[步骤]` → 验证: `[检查]`

强有力的成功标准让 LLM 能够独立循环执行。弱标准（"让它工作"）需要不断澄清。